export interface Education {
    title: string;
    description: string;
    certificate?: string;
}

export interface Experience {
    description: string;
}

export type Format = "video" | "in-person"

